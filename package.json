{
    "name": "query-of-t",
    "version": "1.0.0",
    "private": true,
    "description": "",
    "type": "module",
    "packageManager": "pnpm@11.9.0",
    "scripts": {
        "build:transformer": "pnpm --filter quote-transformer build",
        "build:types": "tspc -b",
        "watch:types": "tspc -b --watch",
        "build": "pnpm run build:transformer && pnpm run build:types && pnpm --filter eastwind build",
        "dev": "pnpm --filter eastwind dev"
    },
    "author": "Olmo del Corral",
    "license": "MIT",
    "dependencies": {
        "pg": "^8.11.3"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "ts-patch": "^4.0.1",
        "typescript": "^6.0.3"
    }
}
