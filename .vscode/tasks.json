{
	"version": "2.0.0",
	// All builds go through the root yarn-workspace scripts (see package.json).
	// One `yarn install` at the root sets up every workspace — no per-folder installs.
	"tasks": [
		{
			"label": "build",
			"detail": "Full build: transformer → types → eastwind (root: yarn build)",
			"type": "shell",
			"command": "yarn build",
			"options": { "cwd": "${workspaceFolder}" },
			"problemMatcher": ["$tsc"],
			"group": { "kind": "build", "isDefault": true }
		},
		{
			"label": "build types (tspc -b)",
			"detail": "Builds every composite project (entities/logic/react) via the root solution.",
			"type": "shell",
			"command": "yarn build:types",
			"options": { "cwd": "${workspaceFolder}" },
			"problemMatcher": ["$tsc"],
			"group": "build"
		},
		{
			"label": "watch types",
			"detail": "tspc -b --watch — keeps emitted JS fresh for vite dev.",
			"type": "shell",
			"command": "yarn watch:types",
			"options": { "cwd": "${workspaceFolder}" },
			"problemMatcher": ["$tsc"],
			"isBackground": true,
			"group": "build"
		},
		{
			"label": "dev (eastwind)",
			"detail": "Runs eastwind types + server + client watchers (root: yarn dev).",
			"type": "shell",
			"command": "yarn dev",
			"options": { "cwd": "${workspaceFolder}" },
			"problemMatcher": [],
			"isBackground": true,
			"group": "build"
		},
		{
			"label": "build eastwind server bundle",
			"detail": "Bundles transformer-emitted logic JS into one runnable file (used by the server debugger).",
			"type": "shell",
			"command": "yarn workspace eastwind build:server",
			"options": { "cwd": "${workspaceFolder}" },
			"problemMatcher": [],
			"dependsOn": ["build types (tspc -b)"],
			"dependsOrder": "sequence",
			"group": "build"
		},
		{
			"label": "tsc: build - quote-transformer-test",
			"detail": "preLaunchTask for the transformer-test debug configs.",
			"type": "typescript",
			"tsconfig": "altea/quote-transformer-test/tsconfig.json",
			"problemMatcher": ["$tsc"],
			"group": "build"
		}
	]
}
